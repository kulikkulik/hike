import random

# Выбираем случайное число от 1 до 100
number = random.randint(1, 100)

# Задаем начальное значение переменной guess
guess = 0

# Создаем цикл, который продолжается до тех пор, пока guess не равно number
while guess != number:
    # Просим пользователя ввести число и преобразуем его в целое число
    guess = int(input("Угадайте число от 1 до 100: "))

    # Сравниваем число пользователя с загаданным числом и выводим соответствующее сообщение
    if guess < number:
        print("Загаданное число больше")
    elif guess > number:
        print("Загаданное число меньше")
    else:
        print("Вы угадали число!")
